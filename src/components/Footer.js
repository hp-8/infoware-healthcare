import React from 'react';
import './Footer.css'; // Make sure to have a separate CSS file for styling (Footer.css in this case)

const Footer = () => {
  return (
    <footer className="footer">
      <p className='company-name'>Registered by The InfoWare House - 2023</p>
    </footer>
  );
};

export default Footer;
